Reserved for future Claude Code skills and hooks for this repo (for example,
stripping notebook outputs before commit, or checking a cleaning notebook
against `data_dictionary.txt`). Nothing is implemented here yet — see
`AI_WORKFLOW.md` at the repo root for the reasoning.
